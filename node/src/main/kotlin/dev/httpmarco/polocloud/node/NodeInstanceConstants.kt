package dev.httpmarco.polocloud.node

import java.nio.file.Path

val LOCAL_PATH: Path = Path.of("local")

val LOCAL_NODE_PATH: Path = Path.of("local-node.json")

val LOCAL_NODE_ID_PATH: Path = LOCAL_PATH.resolve("node-id")